# Optional. When unset the app runs in fully offline demo mode.
# These are baked into the client bundle at build time: only use keys you are
# comfortable exposing to the browser (e.g. a proxy or a rate-limited key).
VITE_OPENAI_API_KEY=
VITE_OPENAI_BASE_URL=https://api.openai.com/v1
VITE_OPENAI_MODEL=gpt-4o-mini
